<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Yüklenen Fotoğraflar</title>
</head>
<body>
  <h1>Yüklenen Fotoğraflar</h1>

  <div>
    <a href="/secenler-upload">Yeni Fotoğraf Yükle</a>
  </div>

  <div>
    <h2>Fotoğraflar ve Açıklamalar:</h2>
    <% if (photos && photos.length > 0) { %>
      <% photos.forEach(photo => { %>
        <div>
          <img src="data:image/jpeg;base64,<%= photo.photo_data %>" width="200" /><br>
          <strong>Açıklama:</strong> <%= photo.description %>
        </div><br>
      <% }) %>
    <% } else { %>
      <p>Henüz hiç fotoğraf yüklenmedi.</p>
    <% } %>
  </div>

</body>
</html>
